services:
  - type: web
    name: video-render-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    plan: free
    envVars: []
    autoDeploy: true
    region: oregon
